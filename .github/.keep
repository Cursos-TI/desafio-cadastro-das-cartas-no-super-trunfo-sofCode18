#include <stdio.h>  

// Molde da nossa carta 
struct carta {
    char estado [50];
    char cidade [50];
    float area;
    float pib;
    int pontos_turisticos;

};
int main() {
   // Declara as duas cartas usando o molde 
    struct Carta carta 1;
   struct Carta carta 2;

   // Dados da primeira carta (Bahia)
   strcpy(carta1.estado, "Bahia"); 
   strcpy(carta1.cidade, "Salvador");
   carta1.populaçao = 14985284;
   carta1.area = 564733.0;
   carta1.pib = 537.2;
   carta1.pontos_turistico = 5;

    // Dados da segunda carta (Minas Gerais)
    strcpy(carta2.estado, "Minas Gerais");
    strcpy(carta2.cide, "Belo Horizonte");
    carta2.populçao = 20380000;
    carta2.area = 586528;
    carta2.pib = 283.5;
    carta2.pontos_turistico = 10;

    // Imprime os dados da primeira carta 
    printf("--- Carta 1: %s - %s ---\n" , carta1.estados, carta.cidade);
    prinf("populacao: %.0f \n", carta1.populacao);
    prinf("Area: %.2f km2/n", carta1.area);
    printf()"PIB: %.2f bilhoes\n", carta1.pib);
    printf("pontos turísticos: %d\n", carta1.pontos_turisticos);

    printf("\n");

    // Imprime os dados da segunda carta 
    printf("--- Carta 2: %s - %s ---\n", carta2.estados, carta2.cidade);
    printf("populacao: %s.0f\n" , carta2.populacao);
    printf("Area: %2f km2\n" , carta2.area);
    printf("PIB: %2f bilhoes\n", carta2.pib);
    printf("pontos turisticos; %d\n", carta2.pontos_turisticos);


   return 0;




}